# interview-challenge-veed

## Running
- Clone this repo and cd into the directory
- Run `yarn install` to install required dependencies
- Run `yarn start` to run the cli
- Run `yarn test` to run all tests
